# HypeSeek_Core

Core repository for the HypeSeek project.

## Landing page

The landing page lives in `landing/`.

### Run locally

```bash
cd landing
npm install
npm run dev
```

### Build

```bash
cd landing
npm run build
npm run preview
```
